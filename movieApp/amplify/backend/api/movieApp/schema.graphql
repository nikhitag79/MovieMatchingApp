type Match @model @auth(rules: [{allow: public}]) {
  id: ID!
  users: User @hasOne
  User1: User @hasOne
}

enum Gender {
  FEMALE
  MALE
}

type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  bio: String
  image: String
  Gender: Gender
}
